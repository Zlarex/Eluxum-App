<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here.
        <item name="colorPrimary">@color/appBlue</item>
        <item name="appWhite">@color/appWhite</item>
        <item name="appWhiteR">@color/appWhiteR</item>
        <item name="appBlack">@color/appBlack</item>
        <item name="appBlue">@color/appBlue</item>
        <item name="appBlackDisable">@color/appBlackDisable</item>
        <item name="appAqua">@color/appAqua</item>
        -->
    </style>

    <style name="AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="PopupOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

</resources>
